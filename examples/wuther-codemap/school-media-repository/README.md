# School Media Processing

This synthetic repository turns classroom recordings into searchable lesson
pages. It exists only as a public Wuther Codemap fixture.

The pipeline accepts a local media file, creates a transcript, stores lesson
metadata, and publishes a static page. No real student data, credentials, or
external service configuration is present.
