# Driftless Codex closeout adapter

For non-trivial work, use the shared `finish-to-done` entrypoint. Before changing
or closing work, evaluate
`shared/schemas/manager-closeout-routing-policy.json` and collect every required
receipt. Do not invoke the four leaf closeout skills for simple questions,
read-only status, or excluded scope.
